version: "2"

build:
  os: ubuntu-24.04
  tools:
    python: "mambaforge-23.11"

conda:
  environment: environment.yml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py

formats: all
