version: "2"

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-latest"

conda:
  environment: environment.yml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py

formats: all
